#include <stdio.h>

int main()
{
    char *s = "Sonu"; // The Space to  this array is allocated in the heap.
    int i = 0;
    for (i; s[i] != '\0'; i++)
    {
    }
    printf("The length of the String is %d", i);
    return 0;
}